name: "18789"

on:
  schedule:
    - cron: "0 * * * *"
  push:
    branches: [main]

jobs:
  job:
    name: Run Script
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v2
      - uses: jannekem/run-python-script-action@v1
        with:
          script: |
            import os
            print("Directory contents:")
            for f in os.listdir('./18789/save/'):
                print(f)
      - run: ./18789/main.exe 5000
      - uses: jannekem/run-python-script-action@v1
        with:
          script: |
            import os
            print("Directory contents:")
            for f in os.listdir('./18789/save/'):
                print(f)
      - run: git config --global user.email "github-actions[bot]@users.noreply.github.com" && git config --global user.name "github-actions[bot]" && git add ./18789/save && git commit -m "Apply automatic changes" && git push origin main
